{!-- <Context get={to_circles: to_circles}> --}
  <div class="mb-2" x-show="open">
    <Bonfire.UI.Common.MultiselectLive
      id="set_boundaries"
      preloaded_options={@default_circles}
      selected_options={e(to_circles, @default_circles)}
      label={@label}
      form_input_name="circles[]"
      pick_event="Bonfire.UI.Boundaries:select"
      remove_event="Bonfire.UI.Boundaries:deselect"
    />
  </div>
{!-- </Context> --}
